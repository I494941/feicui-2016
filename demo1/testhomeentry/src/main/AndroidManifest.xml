<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testhomeentry">

    <application android:allowBackup="true" android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name" android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MianActivityEntry1">

            <intent-filter>

                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MianActivityEntry2"/>
        <activity android:name=".MianActivityEntry3"/>
        <activity android:name=".MianActivityEntry4"/>
        <activity android:name=".MianActivityEntry5"/>
        <activity android:name=".MianActivity"/>


    </application>

</manifest>
